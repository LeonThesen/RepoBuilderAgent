Repository: {{REPO_NAME}}
Sub-agent: {{SUBAGENT_NAME}}
{{PROMPT_BODY}}
Decide in a single pass from the evidence below. Return keys: thought, signal (list), notes (list). If no evidence matches your signal type, return an empty signal list.

SUMMARY_EVIDENCE:
{{SUMMARY_EVIDENCE}}
