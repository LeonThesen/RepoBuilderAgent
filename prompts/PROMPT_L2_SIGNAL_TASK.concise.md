Repository: {{REPO_NAME}}
Sub-agent: {{SUBAGENT_NAME}}
{{PROMPT_BODY}}
Decide in single pass from evidence below. Return keys: thought, signal (list), notes (list). If no evidence matches your signal type, return empty signal list.

SUMMARY_EVIDENCE:
{{SUMMARY_EVIDENCE}}
